plugins {
    id "java"
    id "org.jetbrains.intellij.platform" version "2.14.0"
}

group = "dev.flowicons"
version = "0.2.0"

repositories {
    mavenCentral()
    intellijPlatform {
        defaultRepositories()
    }
}

dependencies {
    implementation files("libs/gson.jar")
    implementation files("libs/brotli.dec.jar")
    testImplementation "org.junit.jupiter:junit-jupiter:5.10.2"
    testRuntimeOnly "junit:junit:4.13.2"
    testRuntimeOnly "org.junit.platform:junit-platform-launcher"

    intellijPlatform {
        if (project.hasProperty("localIdePath")) {
            local(localIdePath)
        } else {
            intellijIdea("2025.3")
        }
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

intellijPlatform {
    buildSearchableOptions = false
    instrumentCode = false

    pluginConfiguration {
        id = "dev.flowicons.jetbrains"
        name = "Flow Icons"
        version = project.version

        description = """
            Flow Icons file and folder icon theme for JetBrains IDEs.
        """.stripIndent().trim()

        ideaVersion {
            sinceBuild = "253"
        }

        changeNotes = """
            Add SVG icon pack support, all Flow Icons palettes, PSI icon provider coverage, and generated mapping overrides.
        """.stripIndent().trim()
    }
}

tasks.withType(Test).configureEach {
    useJUnitPlatform()
}
